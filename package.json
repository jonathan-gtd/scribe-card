{
  "name": "scribe-card",
  "version": "0.1.0",
  "description": "A Lovelace card that charts whatever SQL you give it, through the Scribe integration.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node build-test.js && node --test test/",
    "screenshot": "node scripts/screenshot.js"
  },
  "dependencies": {
    "lit": "^3.2.1",
    "uplot": "^1.6.32"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "^1.63.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
